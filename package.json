{
  "name": "@dailyquest/finance",
  "version": "0.1.0",
  "description": "Canadian finance constants with enforced provenance. Every value carries its authority, citation, source URL, effective window, and verification status — and cannot be read until a human has checked it.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.6"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test 'src/__tests__/*.test.ts'",
    "queue": "node --experimental-strip-types scripts/verification-queue.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "canada",
    "tax",
    "finance",
    "provenance"
  ],
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
