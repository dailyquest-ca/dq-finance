{
  "name": "@dailyquest/finance",
  "version": "0.2.0",
  "description": "Canadian finance constants with enforced provenance, readable from TypeScript and Python. data/constants.json is the single source of truth; both readers are proven in agreement by a shared conformance suite.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.6"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "data"
  ],
  "scripts": {
    "test": "npm run test:ts && npm run test:py",
    "test:ts": "node --experimental-strip-types --test 'src/__tests__/*.test.ts'",
    "test:py": "cd python && python3 -m pytest -q",
    "queue": "node --experimental-strip-types scripts/verification-queue.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "canada",
    "tax",
    "finance",
    "provenance"
  ],
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
